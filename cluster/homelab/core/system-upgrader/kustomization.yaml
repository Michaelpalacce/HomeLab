---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
    - plans/stable
#    - plans/latest # Switch to latest if desired
    - git:https://github.com/rancher/system-upgrade-controller/tree/v0.9.1 # There is a kustomization spec inside of the git repo, may be good to monitor for new releases in the future
images:
    - name: rancher/system-upgrade-controller
      newTag: v0.9.1 # overwrite since there is a newer version