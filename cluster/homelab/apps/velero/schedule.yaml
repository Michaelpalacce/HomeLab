apiVersion: velero.io/v1
kind: Schedule
metadata:
  name: general
  namespace: velero
spec:
  schedule: '@every 24h'
  template:
    hooks: {}
    includedNamespaces:
      - uptimekuma
      - vaultwarden
      - postgres
    snapshotVolumes: true
    ttl: 168h0m0s
  useOwnerReferencesInBackup: false
