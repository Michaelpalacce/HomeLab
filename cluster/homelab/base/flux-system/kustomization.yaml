apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
    - gotk-components.yaml
    - gotk-sync.yaml
    - capacitor.yaml
    - kube-system-networkPolicy.yaml
patchesStrategicMerge:
    - gotk-patch.yaml
