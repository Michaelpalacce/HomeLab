---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
    name: wireshark
    namespace: {{ $.Release.Namespace }}
    annotations:
        kubernetes.io/ingress.class: "nginx"
spec:
    tls:
        -   hosts:
                - "*.stefangenov.site"
            secretName: ingress
    rules:
        -   host: wireshark.stefangenov.site
            http:
                paths:
                    -   pathType: Prefix
                        path: "/"
                        backend:
                            service:
                                name: wireshark
                                port:
                                    number: {{ .Values.wireshark.port }}
