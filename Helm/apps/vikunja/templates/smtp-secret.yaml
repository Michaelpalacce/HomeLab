{{- with .Values.vikunja.smtp }}
apiVersion: v1
kind: Secret
metadata:
    name: smtp-settings
type: Opaque
stringData:
    enabled: "{{ .enabled }}"
    host: "{{ .host }}"
    port: "{{ .port }}"
    username: "{{ .username }}"
    password: "{{ .password }}"
{{- end }}