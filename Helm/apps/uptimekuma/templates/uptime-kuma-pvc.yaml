---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: uptimekuma-pvc
spec:
    accessModes:
        - ReadWriteOnce
    storageClassName: longhorn
    resources:
        requests:
            storage: {{ .Values.storage.size }}
