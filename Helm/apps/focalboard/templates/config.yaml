---
apiVersion: v1
kind: ConfigMap
metadata:
    name: config
    namespace: {{ $.Release.Namespace }}
data:
    config.json: |
        {
            "dbtype": "postgres",
            "dbconfig": "postgres://focalboard:focalboard@postgresql14.postgresql/boards?sslmode=disable&connect_timeout=10"
        }
